

let initialState = {
    friends: [
        { name: 'Gergunchik' },
        { name: 'Gleb' },
        { name: 'Timur' }
    ],
}

const navbarReducer = (state = initialState) => {
    return state;
}

export default navbarReducer;
